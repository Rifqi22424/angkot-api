datasource db {
  provider    = "postgresql"
  url         = "postgres://postgres.esrdqlfatfwftthiqkmr:sukabumi04082004@aws-0-ap-southeast-1.pooler.supabase.com:6543/postgres"
  directUrl   = "postgres://postgres.esrdqlfatfwftthiqkmr:sukabumi04082004@aws-0-ap-southeast-1.pooler.supabase.com:5432/postgres"
}

generator client {
  provider = "prisma-client-js"
}

model Angkot {
  id        Int      @id @default(autoincrement())
  name      String
  driver    String
  lat       Float
  lng       Float
}

model User {
  id        Int      @id @default(autoincrement())
  name      String
  lat       Float
  lng       Float
}

model Route {
  id        Int      @id @default(autoincrement())
  name      String
  points    Json
}
